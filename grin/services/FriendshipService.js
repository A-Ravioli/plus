const FriendshipService = {
  getMutualFriendsPosts: async (allPosts, currentUsername) => {
    // Dummy implementation: return all posts, in a real scenario, filter based on mutual friendship
    return allPosts;
  }
};

export default FriendshipService; 